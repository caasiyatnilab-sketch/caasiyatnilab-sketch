// Optimized: Removed manual smooth scrolling logic as it's now handled by CSS 'scroll-behavior: smooth'
// and the 'defer' attribute in the script tag ensures it runs after DOM is parsed, making DOMContentLoaded redundant.

console.log('Portfolio website loaded successfully!');
