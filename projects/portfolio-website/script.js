// Redundant smooth scroll logic removed.
// Handled by CSS 'scroll-behavior: smooth' for better performance.
// Script uses 'defer' in index.html, so it runs after DOM is parsed but before DOMContentLoaded.

console.log('Portfolio website loaded successfully!');
