document.addEventListener('DOMContentLoaded', function() {
    // Note: Smooth scrolling is now handled by CSS 'scroll-behavior: smooth' on the html element.
    // This reduces JS execution overhead and provides a smoother experience.
    
    console.log('Portfolio website loaded successfully!');
});
