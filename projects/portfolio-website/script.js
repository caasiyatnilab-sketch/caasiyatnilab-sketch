document.addEventListener('DOMContentLoaded', function() {
    // Removed JavaScript-based smooth scrolling in favor of native CSS scroll-behavior: smooth.
    // This reduces main thread execution and allows the browser to optimize the animation on the compositor thread.
    
    console.log('Portfolio website loaded successfully!');
});