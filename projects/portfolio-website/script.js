// Manual smooth scroll logic removed in favor of native CSS scroll-behavior: smooth
// which is more performant as it runs on the compositor thread.
console.log('Portfolio website loaded successfully!');
