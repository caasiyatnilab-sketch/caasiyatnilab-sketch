document.addEventListener('DOMContentLoaded', function() {
    // Native smooth scrolling is handled by CSS (scroll-behavior: smooth).
    // This allows the browser to handle navigation natively, which is more performant
    // and preserves the URL hash for better shareability and accessibility.
    
    console.log('Portfolio website loaded successfully!');
});
