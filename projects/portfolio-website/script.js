// Note: Smooth scrolling is now handled natively via CSS (scroll-behavior: smooth) in styles.css
// for improved performance and better main-thread availability.
// Using 'defer' in index.html ensures this script runs after parsing without blocking.
console.log('Portfolio website loaded successfully!');
