document.addEventListener('DOMContentLoaded', function() {
    /* ⚡ Bolt: Removed redundant JavaScript smooth scrolling logic.
       Native CSS `scroll-behavior: smooth` is now used for better performance. */
    
    console.log('Portfolio website loaded successfully!');
});